# Server (GCP native)
PORT=3001

# Cloud SQL (PostgreSQL)
PGHOST=localhost
PGPORT=5432
PGUSER=meal_planner
PGPASSWORD=change-me
PGDATABASE=meal_planner

# Or single URL (overrides above)
# DATABASE_URL=postgresql://user:pass@host:5432/meal_planner

# Firebase Admin (verify ID tokens) - one of:
# - FIREBASE_SERVICE_ACCOUNT_JSON = full JSON string of service account key
# - GOOGLE_APPLICATION_CREDENTIALS = path to service account JSON file
# - GCLOUD_PROJECT + Application Default Credentials
GCLOUD_PROJECT=your-gcp-project-id
# FIREBASE_SERVICE_ACCOUNT_JSON={"type":"service_account",...}

# Optional: Vertex AI (Gemini) for chat
# VERTEX_AI_ENABLED=true
# VERTEX_AI_LOCATION=us-central1
# VERTEX_AI_MODEL=gemini-1.5-flash
